# My portfolio website



# Run the app

In a terminal in the project directory:

```
$ npm install
$ npm run start
```

# Deploy the app

In a terminal in the project directory:

```
$ npm run build
$ cp _redirects ./build
```
